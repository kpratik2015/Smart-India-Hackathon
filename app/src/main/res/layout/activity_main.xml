<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/backgd"
    tools:context="com.pratikkataria.hackathon.MainActivity">

    <Button
        android:id="@+id/sqlitedb"
        android:layout_width="101dp"
        android:layout_height="101dp"
        android:text="SQLite"
        android:background="@drawable/database_250"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="42dp"
        android:textColor="#FFFFFF"
        />

    <Button
        android:id="@+id/voltVal"
        android:layout_width="101dp"
        android:layout_height="101dp"
        android:background="@drawable/voltage_icon"
        android:text="Latest Voltage"
        android:layout_marginEnd="30dp"
        android:layout_alignTop="@+id/sqlitedb"
        android:layout_alignParentEnd="true"
        android:textColor="#FFFFFF"/>

    <Button
        android:id="@+id/mapBtn"
        android:layout_width="101dp"
        android:layout_height="101dp"
        android:background="@drawable/map_icon"
        android:text="MAP"
        android:textColor="#FFFFFF"
        android:layout_below="@+id/voltVal"
        android:layout_alignParentStart="true"
        android:layout_marginTop="27dp" />

    <Button
        android:id="@+id/notifyBtn"
        android:layout_width="101dp"
        android:layout_height="101dp"
        android:background="@drawable/notify_icon"
        android:text="NOTIFICATION"
        android:textColor="#FFFFFF"
        android:layout_alignTop="@+id/mapBtn"
        android:layout_alignStart="@+id/voltVal" />

</RelativeLayout>